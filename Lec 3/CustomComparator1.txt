compare(a, b) Java ko bolta hai:
kaun pehle aayega
kaun baad mein

Tumhara code (descending)
if (num1 < num2) return 1;
else if (num1 > num2) return -1;
return 0;

Matlab 👇
num1 < num2 → chhota hai → peeche bhejo
num1 > num2 → bada hai → aage rakho

Isliye bada number pehle aata hai.
Dry run ek line mein
List:
[2, 5, 1]


Java dekhta:
2 vs 5 → 5 pehle
5 vs 1 → 5 pehle
2 vs 1 → 2 pehle

Result:

[5, 2, 1]